h1 Создать колонну

br
= simple_form_for @column, url: category_columns_path(@category) do |f|
  = f.input :name, input_html: { class: 'form-control' }
  .mt-2
    = f.submit 'Создать колонну', class: 'btn btn-success'
